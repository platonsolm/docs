---
title: 'API Authorization'
api: POST api/v1/security/login
description: "Как аутентифицироваться в Merchant API с использованием Bearer токенов JWT."
---
## Login
Аутентифицируйтесь, чтобы получить токен доступа, который можно использовать для последующих запросов.

## Endpoint
```
POST /api/v1/security/login
```

## Request Body

| Parameter | Type | Required | Description |
|----------|-------|----------|-------------|
| ```username```  | string  | yes  | Your username |
| ```password``` | string  | yes  | Your password |
| ```provider``` | string | yes  | Authentication provider (use "db") |

## Response

<CodeGroup>
```json Response
{
    "access_token": "ey......"
}
```
| Parameter | Type | Description |
|----------|-------|-------------|
| ```access_token``` | string | JWT token to be used for authentication |

</CodeGroup>
<ResponseField name="success" type="string">
    Indicates
</ResponseField>

<ParamField body="username" type="string">
    username
</ParamField>

<ParamField body="password" type="string">
    password
</ParamField>

<ParamField body="provider" type="string">
    provider
</ParamField>

## Response
<RequestExample>
```json
{
  "username": "admin",
  "password": "testPassword",
  "provider": "db"
}
```
</RequestExample>

<ResponseField name="access_token" type="ey......">
    This is ...
</ResponseField>
